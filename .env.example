# .env.example (Commit this file)

# Database Credentials (Replace with your local values in .env)
POSTGRES_USER=poll_user_placeholder
POSTGRES_PASSWORD=replace_with_local_password
POSTGRES_DB=poll_db